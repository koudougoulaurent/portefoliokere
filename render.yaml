services:
  - type: web
    name: portfolio-guillaume-kere
    env: static
    buildCommand: echo "Building static site..."
    staticPublishPath: .
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=3600

